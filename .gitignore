# secrets — never commit
.credentials.json
**/.credentials.json
.claude.json
local.env
# overlay clones
overlay/
.overlay/
# python
__pycache__/
*.pyc
.venv/
.pytest_cache/
# machine state (if ever mirrored by accident)
**/projects/
**/history.jsonl
**/sessions/
**/paste-cache/
**/cache/
*.sock
*.pid
# ccpkg backups + caches
*.ccpkg.bak
.ccpkg/cache/
# build output
dist/
# local-only purity denylist (real company/person/product terms — never commit)
.ccpkg/purity-terms.txt
# private overlay (design docs + personal/company content — never commit)
private/
