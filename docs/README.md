# docs/

Public, generic documentation only.

- [`overlay-example/`](./overlay-example/) — a committed, scrubbed example of the
  private-overlay layout (its own `manifest.json` with `layer="overlay"` items plus a
  `home/.claude/` mirror).

Internal design docs (contract, specs, implementation plans) are intentionally **not**
tracked in this public base. They live in the private overlay alongside other
personal/company content.
